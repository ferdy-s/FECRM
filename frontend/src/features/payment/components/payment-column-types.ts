export interface VisibleColumns {
  invoiceNumber: boolean;
  amount: boolean;
  paymentMethod: boolean;
  status: boolean;
  uploadedBy: boolean;
  verifiedBy: boolean;
  createdAt: boolean;
  verifiedAt: boolean;
  action: boolean;
}